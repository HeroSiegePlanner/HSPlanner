## Feature
- Bug report button: if you want report any feedback please use this
- Automatic update check: the app now looks for a new release shortly after launch (at most once every 6h) and shows the badge on its own, on the build library screen as well as inside a build. Toggle it in Settings > Updates, where there is also a manual check
- Removed: auto-install on quit, and Skip This Version. The localStorage keys they left behind are swept once on the next launch
